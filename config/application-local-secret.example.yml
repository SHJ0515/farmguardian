# Documentation-only template. Do not copy it until every required value is ready.
# The real application-local-secret.yml is ignored by Git.
# Prefer scripts/New-LocalJwtSecret.ps1 to create the JWT section safely.

# spring:
#   datasource:
#     username: <local-database-user>
#     password: <local-database-password>

# jwt:
#   secret: <512-bit-base64-secret-generated-by-the-script>

# mqtt:
#   username: <local-mqtt-user>
#   password: <local-mqtt-password>

# Enable Firebase only after configuring Application Default Credentials or an external key path.
# firebase:
#   enabled: true
#   credentials-path: C:/secure-path/firebase-service-account.json
